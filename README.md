create 5file in a directory(touch,nano,vi)
2. write some content in every file
3. initiated git ( git init: for creating local repository, for verify type ls -al showing .git filder ) 
4. git status    (for showing untrack file)
5. git add       (for individual track file)
6. git add.      (for all  track file)
7. git commit -m "your massage"  (for commit all track file)
8. git log       (for showing all commit with brief and commit id)
9. git log --oneline (showing all commit in once)
10. git config --global user.name "user name" (for creating user name)
11. git config --global user.email "mail id" (for creating mail)
12. git config --list   (for verify username & mail id)
13. git remote add origin <repo address> (for connecting GitHub repo)
14. git remote -v (for verify repo)
15. git push origin master  (for push master branch: required user id and password* )*go to setting -developer option-classic token and check 
                                                                                      all the admin check box*




                          TASK  2
 git clone <reop address> (change dir my-application_your name)
 git fetch origin         (for fetching branch)
 git checkout <branch name> (for switch branch)
 git branch                (for showing branch "*"indicate current branch)
 git checkout -b            (for creating new branch and switch branch)
                        TASK 3
Go to your repository on GitHub.
Open a Pull Request (PR) for feature/login → master. Click Merge Pull Request.
Open a Pull Request (PR) for feature/pay → master. Click Merge Pull Request   
                        TASK 4
git branch -d <branch name>(for delete local branch)
git push origin --delete <branch name> (for delete remote branch)                                                                                     
